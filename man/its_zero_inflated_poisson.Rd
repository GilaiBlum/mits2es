% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{its_zero_inflated_poisson}
\alias{its_zero_inflated_poisson}
\title{ITS analysis for zero-inflated count outcomes with multiple interruptions}
\usage{
its_zero_inflated_poisson(
  data,
  form,
  offset_name = NULL,
  time_name,
  intervention_start_indices,
  intervention_lengths = NULL,
  multi_model_type = "singular",
  freq = NULL,
  seasonality = FALSE,
  impact_model = "full",
  counterfactual = FALSE,
  print_summary = FALSE
)
}
\arguments{
\item{data}{The data frame corresponding to the supplied formula, existing of at least 2 variables.}

\item{form}{A formula with the response on the left, followed by the ~ operator, and the covariates on the right.}

\item{offset_name}{A string indicating the name of the offset column in the data, or NULL.}

\item{time_name}{A string giving the name of the time variable.}

\item{intervention_start_indices}{Numeric vector - stating the time point indices of the start of each intervention.}

\item{intervention_lengths}{Numeric vector - stating the duration of each intervention.}

\item{multi_model_type}{A string specifying the multi-disruption architecture: "singular", "multiple", or "mixed". Default is "singular".}

\item{freq}{A positive integer describing the frequency of the time series. Required if seasonality is TRUE.}

\item{seasonality}{Logical - indicating whether seasonal adjustment via Fourier terms should be used. Default is FALSE.}

\item{impact_model}{A string specifying the impact model: "full", "level", or "slope". Default is "full".}

\item{counterfactual}{Logical - indicating whether the counterfactual values should be returned. Default is FALSE.}

\item{print_summary}{Logical - indicating whether the model summary should be printed. Default is FALSE.}
}
\value{
A list with three elements: the fitted zero-inflated model, the model summary (including relative risk), and the data with predictions.
}
\description{
\code{its_zero_inflated_poisson} acts as a wrapper to fit a zero-inflated Poisson 
regression model to an ITS with one or more disruptions, with optional seasonal adjustment.
}
\examples{
data <- zero_inflated_sim_data
form <- as.formula("monthly_total ~ time")
start_indices <- c(which(data$Year==2020 & data$Month==3), which(data$Year==2021 & data$Month==1))
fit <- its_zero_inflated_poisson(data=data, form=form, time_name="time", 
                                 intervention_start_indices=start_indices, freq=12, 
                                 seasonality=TRUE, impact_model="full", counterfactual=TRUE)
}
